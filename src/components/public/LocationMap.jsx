import React from "react";
import { Button } from "@/components/ui/button";
import { X } from "lucide-react";

// Este componente foi simplificado para não exibir o modal
export default function LocationMap({ 
  type, 
  data, 
  title, 
  cities, 
  beaches, 
  businesses, 
  providers,
  onClose,
  onCheckIn,
  latitude,
  longitude,
  name
}) {
  // Componente simplificado que apenas renderiza o conteúdo sem o modal
  return null;
}