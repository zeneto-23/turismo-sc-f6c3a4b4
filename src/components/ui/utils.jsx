import React from "react";

// Função de utilidade para combinar classes CSS
export function cn(...classes) {
  return classes.filter(Boolean).join(" ");
}

// Componente de espaçador para manter consistência com export default
export default function Utils() {
  return null;
}